local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/hololove1021/HolonHUB/refs/heads/main/source.txt')))()

-- 二重起動防止
pcall(function()
    if game:GetService("CoreGui"):FindFirstChild("Orion") then 
        game:GetService("CoreGui").Orion:Destroy() 
    end
end)

local Window = OrionLib:MakeWindow({
    Name = "Advanced Grab Mod v6", 
    HidePremium = false, 
    SaveConfig = true, 
    ConfigFolder = "GrabModConfig",
    IntroEnabled = false
})

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local localPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera

-- --- 変数管理 ---
_G.AntiKickEnabled = false
_G.GrabKillEnabled = false
_G.GrabNoclipEnabled = false
_G.SuperThrowEnabled = false
_G.ThrowPower = 500 -- デフォルトの投げ強度

-- --- 家リセット用変数 ---
_G.PlotReturnEnabled = false
local plotNames = {
    [1] = "緑の家", [2] = "ピンクの家", [3] = "紫の家", [4] = "水色の家", [5] = "桜の家"
}
local houseCoords = {
    ["桜の家"] = CFrame.new(548, 123, -73),
    ["水色の家"] = CFrame.new(509, 83, -338),
    ["紫の家"] = CFrame.new(255, -7, 449),
    ["緑の家"] = CFrame.new(-534, -7, 89),
    ["ピンクの家"] = CFrame.new(-485, -7, -163)
}

-- --- 情報表示用GUI ---
local InfoGui = Instance.new("ScreenGui")
InfoGui.Name = "HomeResetInfo"
if game:GetService("CoreGui"):FindFirstChild("HomeResetInfo") then game:GetService("CoreGui").HomeResetInfo:Destroy() end
InfoGui.Parent = game:GetService("CoreGui")

local InfoLabel = Instance.new("TextLabel")
InfoLabel.Name = "Status"
InfoLabel.Size = UDim2.new(0, 250, 0, 60)
InfoLabel.Position = UDim2.new(0.5, -125, 0.05, 0)
InfoLabel.BackgroundTransparency = 0.6
InfoLabel.BackgroundColor3 = Color3.new(0, 0, 0)
InfoLabel.TextColor3 = Color3.new(1, 1, 1)
InfoLabel.TextSize = 18
InfoLabel.Font = Enum.Font.SourceSansBold
InfoLabel.Parent = InfoGui
InfoLabel.Visible = false

-- --- 家リセットループ ---
task.spawn(function()
    while true do
        task.wait(1)
        if _G.PlotReturnEnabled then
            local char = localPlayer.Character
            local root = char and char:FindFirstChild("HumanoidRootPart")
            
            if root then
                local found = false
                local plots = Workspace:FindFirstChild("Plots")
                if plots then
                    for i = 1, 5 do
                        local plot = plots:FindFirstChild("Plot" .. i)
                        if plot then
                            local sign = plot:FindFirstChild("PlotSign")
                            local owners = sign and sign:FindFirstChild("ThisPlotsOwners")
                            local valObj = owners and owners:FindFirstChild("Value")
                            
                            if valObj and valObj:IsA("StringValue") and valObj.Value == localPlayer.Name then
                                found = true
                                local houseName = plotNames[i] or "Unknown"
                                local timeObj = valObj:FindFirstChild("TimeRemainingNum")
                                local timeVal = timeObj and timeObj.Value or 0
                                
                                InfoLabel.Visible = true
                                InfoLabel.Text = "家: " .. houseName .. "\n残り時間: " .. timeVal .. "秒"
                                
                                if timeVal <= 5 and timeVal > 0 then
                                    local targetCF = houseCoords[houseName]
                                    if targetCF then
                                        local oldCF = root.CFrame
                                        root.CFrame = targetCF
                                        task.wait(0.2)
                                        root.CFrame = oldCF
                                        task.wait(2) -- 連続テレポート防止
                                    end
                                end
                                break
                            end
                        end
                    end
                end
                
                if not found then
                    InfoLabel.Visible = true
                    InfoLabel.Text = "家が見つかりません\n(Plotに入ってください)"
                end
            end
        else
            InfoLabel.Visible = false
        end
    end
end)

local lastGrabbedPart = nil
local originalCollisions = {}

-- --- 通知イベントの取得 ---
local function getGrabNotify()
    local ps = localPlayer:WaitForChild("PlayerScripts", 10)
    if not ps then return nil end
    local cbm = ps:WaitForChild("CharacterAndBeamMove", 10)
    if not cbm then return nil end
    return cbm:WaitForChild("GrabNotifyEvent", 10)
end

local grabNotify = getGrabNotify()

-- --- ターゲット特定ロジック ---
local function findGrabbedTarget()
    local grabParts = Workspace:FindFirstChild("GrabParts")
    if grabParts then
        local grabPart = grabParts:FindFirstChild("GrabPart")
        if grabPart then
            local weld = grabPart:FindFirstChild("WeldConstraint")
            if weld and weld.Part1 then
                return weld.Part1
            end
        end
    end
    return nil
end

-- --- メインロジック監視 ---
if grabNotify then
    grabNotify.Event:Connect(function(isGrabbing)
        if isGrabbing then
            task.wait(0.05)
            local target = findGrabbedTarget()
            if target then
                lastGrabbedPart = target
                
                -- 1. キル処理 (独立トグル)
                if _G.GrabKillEnabled then
                    local char = target.Parent
                    local hum = char:FindFirstChild("Humanoid") or char.Parent:FindFirstChild("Humanoid")
                    if hum then hum.Health = 0 end
                end

                -- 2. 壁すり抜け処理 (独立トグル)
                if _G.GrabNoclipEnabled then
                    local root = target.Parent:IsA("Model") and target.Parent or target
                    pcall(function()
                        for _, p in pairs(root:GetDescendants()) do
                            if p:IsA("BasePart") then
                                originalCollisions[p] = p.CanCollide
                                p.CanCollide = false
                            end
                        end
                    end)
                end
            end
        else
            -- 離した時 (投げた時)
            if lastGrabbedPart then
                -- 3. スーパースロー処理
                if _G.SuperThrowEnabled then
                    pcall(function()
                        -- 物理演算の主導権があるうちに力を加える
                        local force = Camera.CFrame.LookVector * _G.ThrowPower
                        lastGrabbedPart:ApplyImpulse(force * lastGrabbedPart:GetMass())
                    end)
                end

                -- 当たり判定の復元
                for part, state in pairs(originalCollisions) do
                    if part and part.Parent then
                        pcall(function() part.CanCollide = state end)
                    end
                end
                
                originalCollisions = {}
                -- 投げた後に少し待ってから参照を消す
                task.delay(0.1, function() lastGrabbedPart = nil end)
            end
        end
    end)
end

-- --- NinjaKunai (アンチキック) ロジック ---
local function spawnKunai()
    local hrp = localPlayer.Character and localPlayer.Character:FindFirstChild("HumanoidRootPart")
    if hrp then
        local rf = ReplicatedStorage:FindFirstChild("MenuToys") and ReplicatedStorage.MenuToys:FindFirstChild("SpawnToyRemoteFunction")
        if rf then rf:InvokeServer("NinjaKunai", hrp.CFrame, Vector3.zero) end -- 自分の位置に出現
    end
end

-- --- UI構成 ---
local MainTab = Window:MakeTab({ Name = "Main", Icon = "rbxassetid://4483345998" })

-- アンチキック
MainTab:AddToggle({
    Name = "アンチキック (NinjaKunai)",
    Default = false,
    Callback = function(v)
        _G.AntiKickEnabled = v
        if v then
            spawnKunai()
            task.spawn(function()
                while _G.AntiKickEnabled do
                    local toys = Workspace:FindFirstChild(localPlayer.Name .. "SpawnedInToys")
                    local hrp = localPlayer.Character and localPlayer.Character:FindFirstChild("HumanoidRootPart")
                    if hrp and toys then
                        for _, t in pairs(toys:GetChildren()) do
                            if t.Name == "NinjaKunai" then
                                pcall(function()
                                    local root = t:IsA("BasePart") and t or (t:IsA("Model") and (t.PrimaryPart or t:FindFirstChildWhichIsA("BasePart")))
                                    if root and t:IsA("Model") and not t.PrimaryPart then t.PrimaryPart = root end

                                    local parts = t:GetDescendants()
                                    if t:IsA("BasePart") then table.insert(parts, t) end

                                    for _, p in pairs(parts) do
                                        if p:IsA("BasePart") then
                                            p.CanCollide = false
                                            p.CanQuery = false
                                            p.CanTouch = false
                                            p.Anchored = false 
                                            p.Massless = true
                                        end
                                    end

                                    if root then
                                        if root:FindFirstChild("OwnerWeld") then root.OwnerWeld:Destroy() end
                                        
                                        if t:IsA("Model") then t:PivotTo(hrp.CFrame)
                                        else t.CFrame = hrp.CFrame end

                                        root.AssemblyLinearVelocity = hrp.AssemblyLinearVelocity
                                        root.AssemblyAngularVelocity = hrp.AssemblyAngularVelocity
                                    end
                                end)
                            end
                        end
                    end
                    RunService.Stepped:Wait()
                end
            end)
        end
    end
})

local HomeTab = Window:MakeTab({ Name = "Home Reset", Icon = "rbxassetid://4483345998" })

HomeTab:AddToggle({
    Name = "自動家検知＆リセット",
    Default = false,
    Callback = function(v) 
        _G.PlotReturnEnabled = v 
        if not v then InfoLabel.Visible = false end
    end
})

local GrabTab = Window:MakeTab({ Name = "Grab Mod", Icon = "rbxassetid://4483345998" })

GrabTab:AddToggle({
    Name = "掴んだらキル",
    Default = false,
    Callback = function(v) _G.GrabKillEnabled = v end
})

GrabTab:AddToggle({
    Name = "掴んでいる間すり抜け (Noclip)",
    Default = false,
    Callback = function(v) _G.GrabNoclipEnabled = v end
})

GrabTab:AddSection({ Name = "Throw Settings" })

GrabTab:AddToggle({
    Name = "スーパースロー (投げ飛ばし)",
    Default = false,
    Callback = function(v) _G.SuperThrowEnabled = v end
})

GrabTab:AddSlider({
    Name = "投げ飛ばす強さ",
    Min = 0,
    Max = 5000,
    Default = 500,
    Color = Color3.fromRGB(255, 255, 255),
    Increment = 100,
    ValueName = "Power",
    Callback = function(v) _G.ThrowPower = v end
})

OrionLib:Init()
