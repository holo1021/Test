local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/hololove1021/HolonHUB/refs/heads/main/source.txt')))()

-- 二重起動防止
pcall(function()
    if game:GetService("CoreGui"):FindFirstChild("Orion") then 
        game:GetService("CoreGui").Orion:Destroy() 
    end
end)

local Window = OrionLib:MakeWindow({
    Name = "Anti-Kick Only", 
    HidePremium = false, 
    SaveConfig = true, 
    ConfigFolder = "AntiKickSimple",
    IntroEnabled = false
})

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer

-- 変数管理
_G.AntiKickEnabled = false

-- --- NinjaKunai生成ロジック (お腹の位置) ---
local function spawnKunaiAtStomach()
    local character = localPlayer.Character
    local hrp = character and character:FindFirstChild("HumanoidRootPart")
    
    if hrp then
        local spawnToyRF = ReplicatedStorage:WaitForChild("MenuToys"):WaitForChild("SpawnToyRemoteFunction")
        
        -- お腹あたりの位置を計算
        -- HRP(腰)から少し上(0.3)に上げ、少し前(-0.5)に出すことでお腹に重なります
        local stomachCFrame = hrp.CFrame * CFrame.new(0, 0.3, -0.5)
        
        spawnToyRF:InvokeServer(
            "NinjaKunai", 
            stomachCFrame, 
            Vector3.new(0, 0, 0)
        )
    end
end

-- --- UI構成 ---
local Tab = Window:MakeTab({
    Name = "Anti-Kick",
    Icon = "rbxassetid://4483345998"
})

Tab:AddToggle({
    Name = "アンチキック開始 (NinjaKunai)",
    Default = false,
    Callback = function(Value)
        _G.AntiKickEnabled = Value
        
        if Value then
            OrionLib:MakeNotification({
                Name = "System",
                Content = "お腹の位置でクナイの生成を開始しました",
                Time = 3
            })
            
            task.spawn(function()
                while _G.AntiKickEnabled do
                    pcall(spawnKunaiAtStomach)
                    -- アンチキックとして機能させるため、一定間隔で生成
                    task.wait(8) 
                end
            end)
        end
    end
})

Tab:AddLabel("※同期機能(Sync)は削除済みです。")
Tab:AddLabel("※クナイはお腹の位置に生成され、その場に残ります。")

OrionLib:Init()
